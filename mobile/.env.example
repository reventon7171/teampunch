# Base URL of the backend API (see ../backend). Must be reachable from your phone/emulator —
# "localhost" only works in the iOS simulator; for a physical device or Android emulator use your
# computer's LAN IP, e.g. http://192.168.1.50:4000
EXPO_PUBLIC_API_URL="http://localhost:4000"
